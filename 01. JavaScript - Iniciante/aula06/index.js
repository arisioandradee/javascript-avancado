// string, number, undefined, null, boolean, symbol
const nome = 'Arisio';
const sobrenome = 'Andrade';
const idade = 21; //number
const peso = 80.01; //number float

let nomeAluno; //undefined = nao aponta para nenhum local na memoria
nomeAluno = 'João';
let sobrenomeAluno = null;
//console.log(nomeAluno)

const aprovado = true; //boolean = true, false (lógico)

console.log(typeof nomeAluno, sobrenomeAluno, peso, aprovado)